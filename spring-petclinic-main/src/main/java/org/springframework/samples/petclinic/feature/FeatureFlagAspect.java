package org.springframework.samples.petclinic.feature;

/**
 * Placeholder removed Aspect. AOP was removed from dependencies; feature checks
 * are performed via direct calls to `FeatureFlags.isEnabled(...)` inside controllers.
 */
public final class FeatureFlagAspect {
    private FeatureFlagAspect() {}
}
